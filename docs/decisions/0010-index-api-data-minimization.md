# 索引 API 最小化公开数据

- 状态：已接受
- 日期：2026-09-10

## 背景

管理器面向可信局域网开放浏览，但扫描服务的内部数据包含绝对项目路径和资产目录。研发浏览需求不需要获得这些本机文件系统信息。

## 决策

`/api/index` 与状态更新接口的响应只返回项目名称、需求文档和不含绝对路径的资产引用键。服务端将资产目录和状态写入目标保留在内存映射中，图片读取和状态写入均通过引用键解析。

## 后果

局域网访问者可以继续读取需求、图示与最终状态，但无法从索引响应获知主机用户名或项目绝对位置。引用键是服务内部索引标识，不应被视为稳定的跨项目持久化 ID。
