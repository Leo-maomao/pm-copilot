# 移除需求摘要字段

- 状态：已接受
- 日期：2026-09-14

## 背景

需求摘要不在管理器中展示，且搜索已改为只匹配用户可见的标题与正文。继续保存摘要会形成用户无法核验的重复内容，并在迁移时造成元数据污染。

## 决策

从 `requirement.md` front matter、核心数据模型、管理器服务和插件 MCP 接口中移除 `summary`。既有本机需求文件同步删除该字段；标题和 Markdown 正文是需求的唯一文字检索来源。

## 后果

新建和更新需求不再要求或生成摘要。旧文件即使残留该字段也不会影响解析，但下次写入会自动移除。
