cycle_id: "2026-09-02-trace-prompt-evidence-budget"
date: "2026-09-02"
goal: "Prevent Trace Agent stream failures caused by replaying historical controller transcripts."
practice_source:
  source_type: "live_retry_failure"
  source_run_id: "video-editor-canvas-optimizations-2026-08-19"
  source_output_folder: "external host project"
  user_corrections:
    - "Fix the stream-disconnect cause rather than masking it with a longer timeout."
delivery_check_report: "A Trace Agent received 58,157 input tokens, copied the template, then reconnected three times without completing the staged YAML write."
generalized_failures:
  - failure_code: "trace_prompt_replays_historical_provider_transcripts"
    generalized_symptom: "A compact trace prompt includes all retained Agent errors and revision history, causing the mechanical final stage to receive an unbounded context."
    host_specific_details_removed: true
    reusable_rule: "Trace prompts and exported Agent evidence contain one latest attributable completion per required phase plus bounded diagnostics; durable controller history remains outside the model prompt."
input_cases:
  - eval_case: "interactive-trace-stream-reconnect"
    run_id: "video-editor-canvas-optimizations-2026-08-19"
    context_mode: "repo-backed"
    output_folder: "external host project"
artifact_expectations:
  prd: false
  ui_delivery: false
  tracking_plan: false
  dev_tasks: false
  launch_decision: false
  pre_clarification_only: false
baseline:
  scorecard_report: ""
  validation_pass_rate: null
  high_severity_failures:
    - "Trace Agent response stream repeatedly disconnected before it could finish a small YAML artifact."
fixes:
  - path: "scripts/run_interactive_request.py"
    change: "Build a bounded trace evidence index and use it for both prompt construction and run-log export."
    rationale: "Keeps proof of provider/model attribution while removing unrelated historical transcripts from the delivery request."
    fix_surface: "controller"
  - path: "scripts/test_run_interactive_request.py"
    change: "Add regression coverage for a large historical provider transcript."
    rationale: "Prevents reintroducing unbounded Trace Agent prompt growth."
    fix_surface: "test"
regression_updates:
  evals_added_or_updated: []
  validators_added_or_updated:
    - "scripts/test_run_interactive_request.py"
versioning:
  previous_version: "6.2.56"
  new_version: "6.2.57"
  changelog_updated: true
sync:
  embedded_copies_checked:
    - "Repository checkout is the source of truth."
  embedded_copies_updated: []
  skipped_targets:
    - path: "origin/main"
      reason: "Push follows local validation and commit creation."
after:
  scorecard_report: ""
  validation_pass_rate: null
  high_severity_failures: []
decision:
  status: "improved"
  next_action: "Synchronize the global runtime and retry the canonical run through the controller."
