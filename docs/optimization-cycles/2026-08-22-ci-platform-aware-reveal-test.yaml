id: ci-platform-aware-reveal-test-2026-08-22
date: 2026-08-22
goal: "Keep the PRD manager regression suite valid on the Linux GitHub Actions runner and macOS development environment."
source_run:
  source_checkout: "current PM Copilot source checkout"
  previous_runtime_version: "6.2.1"
  target_version: "6.2.2"
generalized_failure: "The reveal endpoint test assumed macOS Finder behavior and failed on Linux even though the implementation correctly returned the documented not-implemented response."
fix_surface:
  - scripts/test_prd_manager.py
validation:
  - "python3 -m unittest discover -s scripts -p 'test_prd_manager.py'"
  - "validate_runtime_routing.py"
  - "validate_repo.py"
  - "git diff --check"
version_change: "6.2.1 -> 6.2.2"
remote_push_status: "pending until main is pushed"
embedded_copy_sync_targets:
  global_runtime: "~/.agents/pm-copilot"
  global_skill: "~/.agents/skills/pm-copilot"
stop_condition: "The reveal test asserts platform-specific behavior without weakening the macOS subprocess safety check."
