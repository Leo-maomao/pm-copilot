id: ci-deterministic-model-fixtures-2026-08-22
date: 2026-08-22
goal: "Make direct Codex runtime regression tests deterministic on clean CI runners."
source_run:
  source_checkout: "current PM Copilot source checkout"
  previous_runtime_version: "6.2.2"
  target_version: "6.2.3"
generalized_failure: "Direct Codex tests inherited the developer machine's model catalog; a clean GitHub runner therefore returned blocked before exercising artifact timeout behavior."
fix_surface:
  - scripts/test_agent_runtime.py
validation:
  - "direct Codex tests with an empty CODEX_HOME"
  - "validate_runtime_routing.py"
  - "validate_repo.py"
  - "git diff --check"
version_change: "6.2.2 -> 6.2.3"
remote_push_status: "pending until main is pushed"
embedded_copy_sync_targets:
  global_runtime: "~/.agents/pm-copilot"
  global_skill: "~/.agents/skills/pm-copilot"
stop_condition: "Unit tests provide explicit model capability fixtures while production execution still requires a real discovered or user-declared model."
