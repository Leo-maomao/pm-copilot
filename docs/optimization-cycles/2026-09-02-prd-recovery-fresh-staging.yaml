cycle_id: "2026-09-02-prd-recovery-fresh-staging"
date: "2026-09-02"
goal: "Ensure an explicitly confirmed PRD recovery reruns delivery in fresh staging."
practice_source:
  source_type: "user_feedback"
  source_run_id: "not_available"
  source_output_folder: ""
  user_corrections:
    - "A retry must create fresh delivery staging and execute the available fallback model instead of returning stale validation results."
  delivery_check_report: "The controller returned recovery_required from an earlier staged validation without creating a new fallback-model call."
observed_corrections:
  - correction: "Recovery state is not valid acceptance evidence for a new explicit retry."
    affected_artifact: "Interactive PRD controller state"
    evidence: "A confirmed retry observed stale run-log and validation state before Seawork startup."
    severity: "High"
generalized_failures:
  - failure_code: "confirmed_recovery_reuses_stale_delivery_acceptance"
    generalized_symptom: "An explicit delivery retry can short-circuit on failed artifacts or accepted-stage records from a prior attempt."
    host_specific_details_removed: true
    reusable_rule: "Before fresh delivery staging, a confirmed recovery clears stage acceptance, validation, recovery metadata, and non-discussion delivery artifacts."
input_cases:
  - eval_case: "interactive-prd-confirmed-recovery"
    run_id: "not_available"
    context_mode: "user_feedback"
    output_folder: ""
scenario_portfolio:
  context_mode: "user_feedback"
  product_type: "other"
  pm_task_type: "self_improvement"
  risk_profile: "high"
  platform: "runtime"
  pm_user_maturity: "senior_pm"
artifact_expectations:
  prd: false
  ui_delivery: false
  tracking_plan: false
  dev_tasks: false
  launch_decision: false
  pre_clarification_only: false
baseline:
  scorecard_report: ""
  validation_pass_rate: null
  high_severity_failures:
    - "Confirmed recovery could return old validation before launching a replacement model."
results:
  generated_artifacts: []
  validation_commands:
    - "python3 -m unittest test_run_interactive_request.py test_model_catalog.py test_agent_runtime.py test_validate_agent_trace.py"
    - "python3 scripts/validate_runtime_routing.py"
    - "python3 scripts/validate_repo.py"
  validation_reports: []
  review_scores:
    delivery: null
    prd: null
    metrics_tracking: null
    ui_delivery: null
    delivery_review: null
failure_classification:
  - failure_code: "confirmed_recovery_reuses_stale_delivery_acceptance"
    severity: "High"
    area: "controller-recovery"
    symptom: "The controller returns a prior failure without beginning the replacement-model delivery call."
    evidence: "User-reported retry against a previously failed staged run."
    first_surface_to_fix: "controller"
fixes:
  - path: "scripts/run_interactive_request.py"
    change: "Mark confirmed failed, delivery, and recovery-required states as a new delivery attempt, then clear stale acceptance before creating staging."
    rationale: "Every explicit recovery starts a new attributable delivery sequence and cannot reuse a failed validation result."
    fix_surface: "controller"
  - path: "scripts/test_run_interactive_request.py"
    change: "Cover clearing stale stage acceptance, validation, recovery metadata, and old delivery artifacts."
    rationale: "Prevent retries from short-circuiting before an Agent call."
    fix_surface: "test"
regression_updates:
  evals_added_or_updated: []
  validators_added_or_updated:
    - "scripts/test_run_interactive_request.py"
versioning:
  previous_version: "6.2.47"
  new_version: "6.2.48"
  changelog_updated: true
sync:
  embedded_copies_checked:
    - "Repository checkout is the current PM Copilot source of truth."
  embedded_copies_updated: []
  skipped_targets:
    - path: "origin/main"
      reason: "Push follows local validation and commit creation."
after:
  scorecard_report: ""
  validation_pass_rate: null
  high_severity_failures: []
decision:
  status: "improved"
  next_action: "Run the confirmed retry and verify delivery calls record a device-discovered fallback model."
