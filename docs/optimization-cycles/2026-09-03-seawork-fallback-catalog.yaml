id: seawork-fallback-catalog-2026-09-03
date: 2026-09-03
goal: "Keep Seawork model fallback usable when a transient catalog probe fails after an Agent error."
source_run:
  source_checkout: "current PM Copilot source checkout"
  previous_runtime_version: "6.2.65"
  target_version: "6.2.67"
generalized_failure: "A terminal Seawork Agent error left no live fallback candidates when the daemon catalog probe was temporarily unavailable, so the controller returned an unattributable failed call despite a recorded device model catalog."
fix_surface:
  - scripts/agent_runtime.py
  - scripts/test_agent_runtime.py
validation:
  - "46 agent runtime regression tests"
  - "validate_runtime_routing.py"
  - "validate_repo.py"
version_change: "6.2.66 -> 6.2.67"
remote_push_status: "pending until main is pushed"
embedded_copy_sync_targets:
  - "global PM Copilot runtime copy"
stop_condition: "A Seawork Agent error either tries a distinct model from the live catalog or falls back to the catalog captured in the failed call before returning recovery_required."
