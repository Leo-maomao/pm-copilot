id: seawork-inspect-contract-2026-09-04
date: 2026-09-04
goal: "Keep detached Agent attribution stable across Seawork inspect JSON schema variants without weakening artifact verification."
source_run:
  source_kind: "confirmed PRD revision interrupted during its first content stage"
  previous_runtime_version: "6.2.103"
  target_version: "6.2.104"
  evidence: "A valid Seawork inspect response used PascalCase Id, Provider, Model, and Status fields; the controller only recognized lowercase fields and quarantined an active stage as unconfirmed."
generalized_failure: "Provider control-plane adapters must normalize documented schema variants before applying strict identity, model, lifecycle, and artifact checks. Recovery guidance must not replay a transport that has already failed, and a normal no-parent controller launch must select a verified direct runtime with a usable local model before it considers a remote scheduler."
fix_surface:
  - scripts/agent_runtime.py
  - scripts/run_interactive_request.py
  - scripts/prd_request_controller.py
  - scripts/test_agent_runtime.py
  - scripts/test_run_interactive_request.py
validation:
  - "PascalCase Seawork inspect record regression"
  - "PascalCase record for a different Agent remains rejected"
  - "Detached Agent attribution and terminal-state regression"
  - "Recovery retry action reselects the current device runtime"
  - "No-parent runtime selection avoids a Seawork probe when a direct model-capable CLI is ready"
  - "Focused runtime and interactive controller test suites"
  - "Repository and runtime-routing validation"
version_change: "6.2.103 -> 6.2.104"
embedded_copy_sync_targets:
  - "global PM Copilot runtime copy"
  - "Codex PM Copilot plugin cache"
remote_push_status: "pending until main is pushed"
sync:
  embedded_copies_checked:
    - "global PM Copilot runtime copy"
    - "Codex PM Copilot plugin cache"
  embedded_copies_updated: []
  skipped_targets: []
stop_condition: "An Agent may advance only after an exact normalized ID match, matching provider/model evidence, terminal lifecycle evidence, and a changed staged artifact; unknown or mismatched records remain isolated."
