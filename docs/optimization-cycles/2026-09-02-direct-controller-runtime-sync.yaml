cycle_id: "2026-09-02-direct-controller-runtime-sync"
date: "2026-09-02"
goal: "Ensure direct global-controller invocation cannot silently bypass PM Copilot runtime synchronization."
practice_source:
  source_type: "user_feedback"
  source_run_id: "not_available"
  source_output_folder: ""
  user_corrections:
    - "A version mismatch should synchronize before execution."
  delivery_check_report: "A directly invoked global controller used an older copied runtime although the source checkout contained the required delivery-integrity fix."
observed_corrections:
  - correction: "Runtime freshness enforcement must protect every production controller entry point, not only plugin activation."
    affected_artifact: "Interactive PRD controller"
    evidence: "The direct global script path bypassed the existing activation-time check."
    severity: "Critical"
generalized_failures:
  - failure_code: "direct_controller_bypasses_global_runtime_sync"
    generalized_symptom: "A copied global controller can execute stale logic when called as a script, even though an activation wrapper would synchronize it."
    host_specific_details_removed: true
    reusable_rule: "Perform runtime freshness validation at the controller boundary; after a successful sync, replace the current process with the synchronized entry point and original arguments."
input_cases:
  - eval_case: "global-controller-runtime-sync"
    run_id: "not_available"
    context_mode: "user_feedback"
    output_folder: ""
scenario_portfolio:
  context_mode: "user_feedback"
  product_type: "other"
  pm_task_type: "self_improvement"
  risk_profile: "operations"
  platform: "non_ui"
  pm_user_maturity: "senior_pm"
artifact_expectations:
  prd: false
  ui_delivery: false
  tracking_plan: false
  dev_tasks: false
  launch_decision: false
  pre_clarification_only: false
baseline:
  scorecard_report: ""
  validation_pass_rate: null
  high_severity_failures:
    - "Global runtime version could lag the source checkout during a direct production invocation."
results:
  generated_artifacts: []
  validation_commands:
    - "python3 -m unittest test_run_interactive_request.py test_agent_runtime.py test_model_catalog.py test_validate_agent_trace.py"
    - "python3 scripts/validate_runtime_routing.py"
    - "python3 scripts/validate_repo.py"
  validation_reports: []
  review_scores:
    delivery: null
    prd: null
    metrics_tracking: null
    ui_delivery: null
    delivery_review: null
failure_classification:
  - failure_code: "direct_controller_bypasses_global_runtime_sync"
    severity: "Critical"
    area: "runtime-entrypoint"
    symptom: "The command uses stale runtime code after its source checkout has been updated."
    evidence: "The installed runtime version differed from the source version used for the published fix."
    first_surface_to_fix: "runtime"
fixes:
  - path: "scripts/run_interactive_request.py"
    change: "Check copied-global runtime freshness before argument parsing; synchronize and exec the updated controller when needed, or fail explicitly when sync is unavailable or blocked."
    rationale: "The executed controller, rather than an optional wrapper, owns the version boundary."
    fix_surface: "controller"
  - path: "scripts/test_run_interactive_request.py"
    change: "Assert that a successful global runtime synchronization restarts the controller with its original arguments."
    rationale: "Prevents regressions that reintroduce stale direct execution."
    fix_surface: "test"
regression_updates:
  evals_added_or_updated: []
  validators_added_or_updated:
    - "scripts/test_run_interactive_request.py"
versioning:
  previous_version: "6.2.52"
  new_version: "6.2.53"
  changelog_updated: true
sync:
  embedded_copies_checked:
    - "Repository checkout is the source of truth."
  embedded_copies_updated: []
  skipped_targets:
    - path: "origin/main"
      reason: "Push follows local validation and commit creation."
after:
  scorecard_report: ""
  validation_pass_rate: null
  high_severity_failures: []
decision:
  status: "improved"
  next_action: "Synchronize the global runtime, then retry the canonical PRD run with the protected controller entry point."
