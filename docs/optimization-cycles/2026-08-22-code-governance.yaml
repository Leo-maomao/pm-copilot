cycle_id: "2026-08-22-code-governance"
date: "2026-08-22"
version_change: "6.0.0 -> 6.0.1"
source_run: "User-directed behavior-preserving deep code governance"
generalized_failure: >-
  Stable Agent policy values and evaluation plan hashing were duplicated across
  runtime controllers, allowing canonical policy ownership to drift.
fix_surface:
  - "scripts/runtime_policy.py"
  - "scripts/portfolio_contract.py"
  - "scripts/agent_runtime.py"
  - "scripts/run_evaluation_scenario.py"
  - "scripts/run_evaluation_portfolio.py"
  - "scripts/audit_evaluation_portfolio.py"
  - "scripts/canonicalize_evaluation_portfolio.py"
regression_update:
  - "scripts/test_runtime_policy.py"
  - "scripts/test_portfolio_contract.py"
canonical_changes:
  - rule: "Adaptive model identifiers and policy id have one repository owner."
    owner: "scripts/runtime_policy.py"
  - rule: "Evaluation plan digest has one deterministic implementation."
    owner: "scripts/portfolio_contract.py"
generalization_boundary:
  - "Provider/session discovery remains runtime-owned by agent_runtime.py."
  - "Timeouts, retries, worker limits, and fixed local tool ports remain separate contracts until their semantics are unified."
  - "External ~/.agents and ~/.codex duplicate skills were audited but not modified."
validation:
  commands:
    - "python3 scripts/validate_runtime_routing.py"
    - "python3 scripts/validate_repo.py"
    - "python3 -m unittest discover -s scripts -p 'test_*.py'"
    - "python3 -m py_compile scripts/*.py"
    - "git diff --check"
  result: "passed"
embedded_copy_sync:
  checked: []
  updated: []
  pending:
    - "Sync/reinstall global runtime only when it is used outside this checkout."
remote_push:
  required: false
  status: "not_requested"
  target: ""
