cycle_id: "2026-09-03-seawork-model-rejection-fallback"
date: "2026-09-03"
goal: "Continue Trace recovery when a discovered Seawork fallback model is rejected upstream."
practice_source:
  source_type: "live_retry_failure"
  source_run_id: "video-editor-canvas-optimizations-2026-08-19"
  source_output_folder: "external host project"
  user_corrections:
    - "Use the next suitable model after a failed model rather than stopping at one fallback."
generalized_failures:
  - failure_code: "single_fallback_model_rejected"
    generalized_symptom: "A locally discovered fallback model receives an upstream 403 before token use, but the runtime stops instead of trying the next device-available model."
    host_specific_details_removed: true
    reusable_rule: "After a transport failure, try a bounded ordered set of current-device candidates and retain each dispatch result; all promotion remains contingent on staged artifact bytes."
input_cases:
  - eval_case: "seawork-upstream-model-rejection"
    run_id: "not_available"
    context_mode: "user_feedback"
    output_folder: ""
artifact_expectations:
  prd: false
  ui_delivery: false
  tracking_plan: false
  dev_tasks: false
  launch_decision: false
  pre_clarification_only: false
baseline:
  scorecard_report: ""
  validation_pass_rate: null
  high_severity_failures: ["The first fallback model was rejected with 403 and prevented further recovery."]
fixes:
  - path: "scripts/agent_runtime.py"
    change: "Try up to three ordered fallback candidates and return the first successful completion with an auditable attempt list."
    rationale: "Provider catalogs can include models temporarily unauthorized for a device or account."
    fix_surface: "runtime"
regression_updates:
  evals_added_or_updated: []
  validators_added_or_updated: ["scripts/test_agent_runtime.py"]
versioning:
  previous_version: "6.2.58"
  new_version: "6.2.59"
  changelog_updated: true
sync:
  embedded_copies_checked: ["Repository checkout is the source of truth."]
  embedded_copies_updated: []
  skipped_targets: []
after:
  scorecard_report: ""
  validation_pass_rate: null
  high_severity_failures: []
decision:
  status: "improved"
  next_action: "Synchronize the global runtime and retry the canonical run."
