cycle_id: "2026-08-03-prd-video-playback"
date: "2026-08-03"
goal: "Preserve user-provided PRD videos as playable evidence instead of first-frame images."
practice_source:
  source_type: "user_feedback"
  observed_problem: "A supplied video figure was rendered as a still frame rather than an inline preview."
canonical_changes:
  - path: "artifacts/prd-contract.md"
    change: "Define user-provided video as playback evidence that preserves the original local video."
    fix_surface: "contract"
  - path: "scripts/render_prd_html.py"
    change: "Convert video references in both link and image syntax into browser video players."
    fix_surface: "renderer"
  - path: "scripts/validate_outputs.py"
    change: "Reject video files rendered as image elements."
    fix_surface: "validator"
regression_updates:
  validators_added_or_updated:
    - "scripts/test_prd_flow_rendering.py"
versioning:
  previous_version: "4.9.9"
  new_version: "4.9.10"
  changelog_updated: true
decision:
  status: "improved"
  next_action: "Verify the next PRD video figure is playable directly in prd.html."
