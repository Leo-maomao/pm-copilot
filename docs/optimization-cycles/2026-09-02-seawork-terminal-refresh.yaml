cycle_id: "2026-09-02-seawork-terminal-refresh"
date: "2026-09-02"
goal: "Recognize an attributable Seawork completion when control-plane terminal state lags artifact delivery."
practice_source:
  source_type: "user_feedback"
  source_run_id: "not_available"
  source_output_folder: ""
  user_corrections:
    - "A completed target file plus a subsequent idle state must be accepted instead of being reverted to recovery_required from a stale running read."
  delivery_check_report: "Terra generated and reviewed the first two artifacts, then the Trace Agent wrote its target while the deadline poll retained running."
observed_corrections:
  - correction: "A deadline-time running read may be stale relative to a detached Agent's terminal record."
    affected_artifact: "Seawork controller completion evidence"
    evidence: "The target file existed and a later control-plane inspection reported idle."
    severity: "High"
generalized_failures:
  - failure_code: "seawork_terminal_state_lag_discards_written_stage"
    generalized_symptom: "A detached Agent can write its promised stage artifact before the controller observes its terminal control-plane state."
    host_specific_details_removed: true
    reusable_rule: "After a polling deadline, perform one non-destructive terminal refresh and accept only a changed promised artifact paired with a successful terminal state."
input_cases:
  - eval_case: "seawork-stage-terminal-refresh"
    run_id: "not_available"
    context_mode: "user_feedback"
    output_folder: ""
scenario_portfolio:
  context_mode: "user_feedback"
  product_type: "other"
  pm_task_type: "self_improvement"
  risk_profile: "high"
  platform: "runtime"
  pm_user_maturity: "senior_pm"
artifact_expectations:
  prd: false
  ui_delivery: false
  tracking_plan: false
  dev_tasks: false
  launch_decision: false
  pre_clarification_only: false
baseline:
  scorecard_report: ""
  validation_pass_rate: null
  high_severity_failures:
    - "A delayed Seawork terminal state could invalidate a written Trace Agent artifact."
results:
  generated_artifacts: []
  validation_commands:
    - "python3 -m unittest test_agent_runtime.py test_run_interactive_request.py test_model_catalog.py test_validate_agent_trace.py"
    - "python3 scripts/validate_runtime_routing.py"
    - "python3 scripts/validate_repo.py"
  validation_reports: []
  review_scores:
    delivery: null
    prd: null
    metrics_tracking: null
    ui_delivery: null
    delivery_review: null
failure_classification:
  - failure_code: "seawork_terminal_state_lag_discards_written_stage"
    severity: "High"
    area: "runtime-polling"
    symptom: "A stale running read causes recovery even though the changed stage artifact and later idle state are attributable."
    evidence: "User-reported Trace Agent recovery failure after successful Terra delivery stages."
    first_surface_to_fix: "runtime"
fixes:
  - path: "scripts/agent_runtime.py"
    change: "Record the target digest before launch, refresh the control-plane state once after a nonterminal poll, and accept only a changed target paired with completed, closed, complete, or idle."
    rationale: "The controller handles a short state propagation delay without accepting a prior staged file or an active Agent."
    fix_surface: "runtime"
  - path: "scripts/test_agent_runtime.py"
    change: "Cover target update plus terminal refresh and retain tests for the still-running timeout path."
    rationale: "Prevent either stale artifact acceptance or premature recovery when the daemon state catches up."
    fix_surface: "test"
regression_updates:
  evals_added_or_updated: []
  validators_added_or_updated:
    - "scripts/test_agent_runtime.py"
versioning:
  previous_version: "6.2.49"
  new_version: "6.2.50"
  changelog_updated: true
sync:
  embedded_copies_checked:
    - "Repository checkout is the current PM Copilot source of truth."
  embedded_copies_updated: []
  skipped_targets:
    - path: "origin/main"
      reason: "Push follows local validation and commit creation."
after:
  scorecard_report: ""
  validation_pass_rate: null
  high_severity_failures: []
decision:
  status: "improved"
  next_action: "Resume the canonical run and verify the Trace Agent records a completed Terra call after terminal refresh."
