cycle_id: "2026-08-22-canonical-prd-governance-v6"
date: "2026-08-22"
version_change: "5.0.3 -> 6.0.0"
source_run: "User-directed PM Copilot self-iteration on first-principles workflow and canonical PRD continuity"
generalized_failure: >-
  The workflow allowed conflicting rule projections and could create duplicate
  PRD directories for revisions, making the canonical result ambiguous.
fix_surface:
  - "PM_COPILOT.md, workflow/context-loading.md, workflow/main-workflow.md"
  - "artifacts/prd-contract.md, skills/prd-writing/SKILL.md, templates"
  - "scripts/prd_manager.py and interactive/evaluation controllers"
  - "validators and regression tests"
user_correction:
  - "A requirement is edited in one identified canonical folder; no -2/-3 revision copies."
  - "One canonical visual and section contract governs every PRD path."
  - "Blockers are recorded once instead of producing conflicting branches."
canonical_changes:
  - rule: "Only --new-requirement creates a PRD folder; --revise updates the identified folder in place."
    owner: "scripts/run_interactive_request.py and scripts/prd_manager.py"
  - rule: "The PRD visual and section contract has one canonical owner and machine projection."
    owner: "artifacts/prd-contract.md and scripts/prd_visual_contract.py"
  - rule: "Evaluation cases reuse one canonical result directory per case."
    owner: "scripts/run_evaluation_scenario.py and scripts/run_evaluation_portfolio.py"
validation:
  commands:
    - "python3 scripts/validate_runtime_routing.py"
    - "python3 scripts/validate_repo.py"
    - "git diff --check"
  result: "passed"
embedded_copy_sync:
  checked: []
  updated: []
  pending:
    - "Reinstall or sync the global runtime if it is used outside this checkout."
remote_push:
  required: false
  status: "not_requested"
  target: ""
