cycle_id: "2026-09-02-seawork-post-stop-artifact-convergence"
date: "2026-09-02"
goal: "Preserve a verified staged artifact when Seawork converges to terminal state during bounded stop cleanup."
practice_source:
  source_type: "user_feedback"
  source_run_id: "not_available"
  source_output_folder: ""
  user_corrections:
    - "A stream-reconnected Trace Agent that wrote the staged target must not be discarded because control-plane state was stale at the deadline."
  delivery_check_report: "The Trace Agent copied its required template into the isolated stage, then reached idle after reconnects; the controller reported a running timeout before considering the post-stop terminal state and changed SHA."
observed_corrections:
  - correction: "Deadline cleanup must combine the post-stop terminal state with a call-local staged digest."
    affected_artifact: "run-log.yaml"
    evidence: "The target SHA changed in the exact stage workspace while the initial deadline read remained running."
    severity: "Critical"
generalized_failures:
  - failure_code: "seawork_post_stop_terminal_artifact_race"
    generalized_symptom: "An Agent writes its target near a deadline but is marked timed out because its terminal state arrives during cleanup."
    host_specific_details_removed: true
    reusable_rule: "After a bounded stop, accept only the same call's changed stage target plus an explicit successful terminal state; reject all other timeout results."
input_cases:
  - eval_case: "seawork-post-stop-artifact-convergence"
    run_id: "not_available"
    context_mode: "user_feedback"
    output_folder: ""
scenario_portfolio:
  context_mode: "user_feedback"
  product_type: "other"
  pm_task_type: "self_improvement"
  risk_profile: "operations"
  platform: "non_ui"
  pm_user_maturity: "senior_pm"
artifact_expectations:
  prd: false
  ui_delivery: false
  tracking_plan: false
  dev_tasks: false
  launch_decision: false
  pre_clarification_only: false
baseline:
  scorecard_report: ""
  validation_pass_rate: null
  high_severity_failures:
    - "Deadline cleanup discarded a staged artifact with a later terminal Agent state."
results:
  generated_artifacts: []
  validation_commands:
    - "python3 -m unittest test_agent_runtime.py test_run_interactive_request.py test_model_catalog.py test_validate_agent_trace.py"
    - "python3 scripts/validate_runtime_routing.py"
    - "python3 scripts/validate_repo.py"
  validation_reports: []
  review_scores:
    delivery: null
    prd: null
    metrics_tracking: null
    ui_delivery: null
    delivery_review: null
failure_classification:
  - failure_code: "seawork_post_stop_terminal_artifact_race"
    severity: "Critical"
    area: "agent-runtime"
    symptom: "An attributable staged artifact is lost after a stale deadline state."
    evidence: "Call-local SHA and post-stop terminal-state disagreement."
    first_surface_to_fix: "runtime"
fixes:
  - path: "scripts/agent_runtime.py"
    change: "Accept only a changed stage target with a successful post-stop terminal state."
    rationale: "Retains valid agent work while preserving strict workspace attribution."
    fix_surface: "runtime"
  - path: "scripts/test_agent_runtime.py"
    change: "Cover a changed artifact that becomes idle only after stop cleanup."
    rationale: "Prevents stale deadline state from regressing into false recovery."
    fix_surface: "test"
regression_updates:
  evals_added_or_updated: []
  validators_added_or_updated:
    - "scripts/test_agent_runtime.py"
versioning:
  previous_version: "6.2.55"
  new_version: "6.2.56"
  changelog_updated: true
sync:
  embedded_copies_checked:
    - "Repository checkout is the source of truth."
  embedded_copies_updated: []
  skipped_targets:
    - path: "origin/main"
      reason: "Push follows local validation and commit creation."
after:
  scorecard_report: ""
  validation_pass_rate: null
  high_severity_failures: []
decision:
  status: "improved"
  next_action: "Synchronize the global runtime and rerun the canonical PRD controller."
