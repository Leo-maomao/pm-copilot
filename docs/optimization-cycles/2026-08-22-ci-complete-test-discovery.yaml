id: ci-complete-test-discovery-2026-08-22
date: 2026-08-22
goal: "Ensure GitHub Actions runs the full Python regression suite instead of silently losing YAML-backed evaluation tests."
source_run:
  source_checkout: "current PM Copilot source checkout"
  previous_runtime_version: "6.2.3"
  target_version: "6.2.4"
generalized_failure: "requirements-dev.txt omitted PyYAML, so the CI runner discovered only 182 tests and failed to import five YAML-backed test modules."
fix_surface:
  - requirements-dev.txt
  - scripts/test_agent_runtime.py
validation:
  - "Python 3.12 test discovery with an empty CODEX_HOME"
  - "validate_runtime_routing.py"
  - "validate_repo.py"
  - "git diff --check"
version_change: "6.2.3 -> 6.2.4"
remote_push_status: "pending until main is pushed"
embedded_copy_sync_targets:
  global_runtime: "~/.agents/pm-copilot"
  global_skill: "~/.agents/skills/pm-copilot"
stop_condition: "CI installs PyYAML before discovery and executes the complete test suite with deterministic model fixtures."
