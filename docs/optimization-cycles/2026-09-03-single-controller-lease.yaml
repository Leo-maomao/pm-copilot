id: single-controller-lease-2026-09-03
date: 2026-09-03
goal: "Prevent repeated confirm invocations from resetting an active delivery workspace."
source_run:
  source_checkout: "canonical video-editor-canvas-optimizations validation run"
  previous_runtime_version: "6.2.76"
  target_version: "6.2.77"
generalized_failure: "Every confirm against delivery/running was treated as a recovery and reset staging, so validation never reached a stable final gate."
fix_surface:
  - scripts/run_interactive_request.py
validation:
  - "36 interactive request regression tests"
  - "validate_runtime_routing.py"
  - "validate_repo.py"
version_change: "6.2.76 -> 6.2.77"
remote_push_status: "pending until main is pushed"
embedded_copy_sync_targets:
  - "global PM Copilot runtime copy"
stop_condition: "A live controller PID causes duplicate confirm to return without resetting staging; dead PID enters recovery first."
