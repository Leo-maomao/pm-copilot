# 安全和隐私

<p align="center"><strong>简体中文</strong> | <a href="docs/en/security-policy.md">English</a></p>

<a id="zh-cn"></a>

PM Copilot 默认本地优先，不包含云服务、数据库、账号系统或遥测。
当前支持的运行时版本为 `6.2.4`；模型提供方配置由用户控制，项目不会把凭证写入产物。

## 支持版本

| 版本 | 支持状态 |
|---|---|
| 6.x | 支持 |
| 5.x | 尽力支持 |

## 报告问题

目前请通过仓库 issue tracker，或项目所有者列出的维护者联系方式，报告安全或隐私问题。

## 敏感数据规则

不要提交：

- 真实密码或 API key
- 完整支付卡号
- 政府证件号码
- 原始个人标识符
- 保密合作方协议
- 私有客户数据
- 未公开财务数据

## Agent 安全期望

使用本仓库的 Agent 应：

- 在处理敏感数据前给出提醒。
- 默认使用合成或匿名化示例。
- 避免在埋点方案中收集不必要的个人属性。
- 清晰标记假设和工具限制。
- 不要编造带来源背书的结论。
- 不要把 Agent 调用记录、模型响应或验证结果伪造成已发生的事实。
- 生成的 `outputs/` 或 `pm-copilot-outputs/` 目录可能包含业务上下文，提交前必须人工检查并脱敏。
